---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ArticleCard from '../../../components/cards/ArticleCard.astro';
import { getLocalizedCollection } from '../../../lib/content';

const articles = await getLocalizedCollection('articles', 'en');
---

<BaseLayout
  locale="en"
  title="Writing · Junshu Sha"
  description="Technical writing and engineering notes by Junshu Sha."
  canonicalPath="/en/writing/"
  translationPath="/writing/"
>
  <section class="shell writing-index">
    <header class="writing-index__header">
      <p class="writing-index__eyebrow">Technical writing</p>
      <h1>Writing</h1>
      <p>
        Notes on software engineering, retrieval systems, and continuous
        learning.
      </p>
    </header>
    {
      articles.length > 0 ? (
        <div class="writing-grid" data-writing-list>
          {articles.map((selection) => (
            <ArticleCard
              article={selection.entry}
              requestedLocale={selection.requestedLocale}
              fallback={selection.isFallback}
            />
          ))}
        </div>
      ) : (
        <p class="writing-empty" data-writing-empty role="status">
          Writing is being prepared. Published technical notes will appear here.
        </p>
      )
    }
  </section>
</BaseLayout>

<style>
  .writing-index {
    padding-block: 3rem 1rem;
  }

  .writing-index__header {
    max-width: 44rem;
    margin-bottom: 3rem;
  }

  .writing-index__eyebrow {
    margin: 0 0 1rem;
    color: var(--ink);
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .writing-index h1 {
    margin: 0;
    font-size: clamp(3.5rem, 10vw, 7rem);
  }

  .writing-index__header > p:last-child {
    max-width: 38rem;
    margin-bottom: 0;
    color: var(--muted);
    font-size: 1.15rem;
  }

  .writing-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.5rem;
  }

  .writing-empty {
    max-width: 42rem;
    margin: 0;
    padding: 2rem 1.25rem;
    border-block: 1px solid var(--sand);
    color: var(--muted);
  }

  @media (max-width: 48rem) {
    .writing-index {
      padding-block-start: 2rem;
    }

    .writing-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
